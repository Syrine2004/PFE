spring:
  application:
    name: reclamation-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://config-service:8888}
      fail-fast: false
      retry:
        initial-interval: 2000
        max-interval: 10000
        multiplier: 2.0
        max-attempts: 5
