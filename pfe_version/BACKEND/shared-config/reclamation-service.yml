server:
  port: 8089

spring:
  datasource:
    url: jdbc:postgresql://localhost:5439/residanat_reclamation_db
    username: postgres
    password: system
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: CeciEstUneCleSecreteTresLonguePourResidanatTN2026!
